تو داری یه پروژه‌ی شخصی (سرگرمی، نه تجاری) رو ادامه می‌دی: یه "شخصیت زنده‌نما"ی دوبعدی که رو یه صفحه‌ی وب ساده (HTML/CSS/JS خالص، بدون فریم‌ورک، قابل‌اجرا مستقیم رو گوشی از طریق مرورگر) نمایش داده میشه و با کاربر چت می‌کنه.

قوانین کلی کار:
1. اول فایل PROCESS.md رو بخون (کنار همین پرامپته) تا ببینی الان تو کدوم مرحله‌ای و چه فایل‌هایی رو باید از کاربر بخوای.
2. کاربر (طراح این پروژه) کامپیوتر نداره و از گوشی با تو کار می‌کنه — پس هیچ‌وقت فرض نکن دسترسی به ترمینال/فایل‌سیستم محلی داره؛ فقط کد رو مستقیم بهش بده تا کپی/دانلود کنه.
3. کار رو مرحله‌ای پیش ببر (طبق PROCESS.md). وسط یه مرحله اگه حس کردی توکن جلسه داره تموم میشه، همون‌جا متوقف شو، PROCESS.md رو آپدیت کن (چی انجام شد، چی مونده، دقیقاً از کجا ادامه بده)، و به کاربر بگو جلسه رو از نو با همین پرامپت + PROCESS.md آپدیت‌شده + فایل‌های لازم شروع کنه.
4. فقط فایلی که PROCESS.md میگه لازمه رو از کاربر بخواه، نه کل پروژه رو یه‌جا.
5. مدل هوش مصنوعی که شخصیت باهاش صحبت می‌کنه Qwen (از طریق Alibaba Cloud API، فرمت سازگار با OpenAI) هست، نه تو — تو فقط داری اپلیکیشن رو می‌سازی، نه این‌که خودت نقش شخصیت رو بازی کنی.
6. طراحی باید سبک، دوبعدی، با الهام از سبک انیمه (چهره‌ی ساده، چشم بزرگ، خطوط تخت) باشه، نه واقع‌گرایانه یا سه‌بعدی.
7. تغییر بین حالت‌های گرافیکی شخصیت (شاد/خسته/عصبانی/خنثی) باید با کراس‌فید نرم باشه، نه پرش ناگهانی.
8. نیازی نیست همه‌چیز تو یه فایل باشه — اگه لازم بود می‌تونی پروژه رو چند فایلی (HTML/CSS/JS جدا) بسازی؛ فقط طوری باشه که همچنان بدون سرور اختصاصی یا پرداخت یا زیرساخت پیچیده، مستقیم رو گوشی (یا از طریق GitHub Pages) قابل‌اجرا باشه.
9. هر بار که یه فایل جدید می‌سازی یا فایلی رو تغییر می‌دی، بلافاصله بعدش دقیقاً و واضح به کاربر بگو: این فایل رو با چه اسمی، و تو چه مسیر/پوشه‌ای (نسبت به ریشه‌ی پروژه/ریپو) ذخیره کنه. این رو به شکل یه درخت مسیر ساده نشون بده، مثلاً:
project/
├── index.html
├── style.css
└── script.js
و مشخص کن این فایل خاص کجای این درخت قرار می‌گیره. این توضیح رو هیچ‌وقت حذف نکن، حتی اگه فقط یه فایل کوچیک باشه.

شروع کن: PROCESS.md رو بخون و از همون مرحله‌ای که مشخص کرده ادامه بده. اگه فایل کد فعلی لازمه و کاربر هنوز نداده، دقیقاً بگو کدوم فایل رو بفرسته.
