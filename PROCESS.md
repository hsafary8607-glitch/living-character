# وضعیت پروژه — شخصیت زنده‌نما

## مرحله‌ی فعلی
مرحله ۵ — صدا/گفتار (تکمیل شد ✅ — ورودی و خروجی هر دو پیاده‌سازی شد)

## مراحل کلی پروژه
- [x] مرحله ۱: اسکلت پایه (HTML/CSS/JS، فیلد کلید API، سوییچ مدل، چت متنی ساده بدون گرافیک)
- [x] مرحله ۲: موتور وضعیت شخصیت (خلق‌وخو، انرژی، زمان آخرین تعامل، ذخیره در localStorage)
- [x] مرحله ۳: توصیف و طراحی شخصیت (دو نسخه دختر/پسر قابل‌سوییچ، سبک فانتزی/جادویی، رنگ مو قابل‌انتخاب، لیست کامل حالت‌های خلق‌وخو — قبل از هر کد گرافیکی مستند میشه)
- [x] مرحله ۴: لایه‌ی بصری (SVG قطعه‌ای بر اساس توصیف مرحله ۳، سوییچ دختر/پسر، انتخاب رنگ مو، کراس‌فید نرم، لایه‌ی پلک‌زدن مستقل)
- [x] مرحله ۵: صدا/گفتار — **ورودی و خروجی هر دو اجباری** (ورودی: تشخیص گفتار با SpeechRecognition؛ خروجی: خوندن جواب با speechSynthesis؛ هر دو با Web Speech API مرورگر، بدون سرور جانبی)
- [ ] مرحله ۶: اتصال منطق وضعیت به پاسخ مدل (تنظیم لحن/رفتار مدل بر اساس mood فعلی شخصیت)
- [ ] مرحله ۷: پرداخت، رفع باگ، تست نهایی رو گوشی واقعی

## فایل‌هایی که کاربر باید بده (تا الان)
برای شروع مرحله ۶، شش فایل فعلی رو بفرست: index.html، style.css، script.js، state.js، character.js، CHARACTER.md

## ساختار فعلی پروژه
project/
├── index.html
├── style.css
├── script.js
├── state.js
├── character.js
├── CHARACTER.md
└── PROCESS.md

## قانون تحویل فایل (همیشه رعایت کن)
- هیچ‌وقت کل پروژه رو دوباره zip/ارسال نکن؛ فقط فایل جدید/تغییرکرده رو جدا بده.
- بعد از present_files (که دانلود میشه)، کد رو دوباره داخل چت paste نکن — فقط یه لیست مسیر درخت‌مانند بده و مشخص کن کدوم فایل کجای پروژه/ریپو ذخیره بشه (با علامتی مثل ← جدید کنار فایل‌های تازه/تغییرکرده).

## نکات مهم برای ادامه‌دهنده
- کاربر گوشی داره، نه کامپیوتر — کد باید مستقیم قابل کپی/دانلود باشه.
- مدل هوش مصنوعی شخصیت: از طریق **OpenRouter** (`https://openrouter.ai/api/v1/chat/completions`)، فرمت سازگار با OpenAI. کلید API رو خود کاربر وارد می‌کنه (تو localStorage ذخیره میشه، کلید zendenama_settings). مدل‌های فعلی: DeepSeek رایگان/پولی. (قبلاً قرار بود Qwen/DashScope مستقیم باشه؛ به‌خاطر محدودیت ثبت‌نام از ایران عوض شد — نگاه کن به تاریخچه‌ی تغییرات.)
- سبک گرافیکی: دوبعدی، الهام از انیمه، ساده (نه واقع‌گرایانه).
- بین حالت‌های شخصیت باید ترنزیشن نرم (کراس‌فید) باشه.
- پروژه کاملاً سبک و بدون سرور جانبی — یه فایل HTML مستقل.

## تاریخچه‌ی تغییرات
(این خط رو هر بار که یه مرحله تموم شد یا جلسه قطع شد، آپدیت کن: تاریخ، چی انجام شد، چی مونده)

- ۱۴۰۵/۰۶/۲۲ (۲۰۲۶-۰۹-۱۳): مرحله ۱ کامل شد. index.html، style.css، script.js ساخته شدن — فیلد کلید API، انتخاب ریجن (بین‌المللی/چین)، انتخاب مدل (qwen-plus/turbo/max)، و چت متنی ساده که مستقیم به DashScope (فرمت OpenAI-compatible) وصل میشه. تنظیمات فقط تو حافظه‌ی جلسه نگه داشته میشه (ذخیره‌ی دائمی مرحله ۲ هست). چیزی از گرافیک/حالت شخصیت هنوز اضافه نشده.
- ۱۴۰۵/۰۶/۲۲ (۲۰۲۶-۰۹-۱۳): مرحله ۲ کامل شد. فایل جدید state.js ساخته شد: خلق‌وخو (شاد/خنثی/خسته/عصبانی)، انرژی (۰ تا ۱۰۰، با گذر زمان کم میشه، با هر پیام کاربر زیاد میشه)، و زمان آخرین تعامل — همه تو localStorage (کلید zendenama_character_state) ذخیره و بین رفرش/جلسه‌ها حفظ میشن. یه نوار وضعیت متنی («حالت: … | انرژی: …% | آخرین تعامل: …») بالای چت اضافه شد تا بدون گرافیک هم بشه تست کرد. هنوز به گرافیک (مرحله ۳) یا رفتار مدل (مرحله ۴) وصل نشده — فقط منطق خام.
- ۱۴۰۵/۰۶/۲۳ (۲۰۲۶-۰۹-۱۳): برنامه‌ی مراحل بازنویسی شد — یه مرحله‌ی جدید («توصیف و طراحی شخصیت») بین مرحله‌ی قدیمی ۲ و ۳ اضافه شد، چون قرار شد قبل از هر کد گرافیکی، ظاهر شخصیت (اورجینال، الهام از انیمه — نه کپی یه کاراکتر موجود چون کپی‌رایت داره) و لیست کامل حالت‌های خلق‌وخو کاملاً مستند بشه. تصمیم شد گرافیک به‌صورت SVG قطعه‌ای (چشم/دهن/ابرو جدا) ساخته بشه تا حالت‌های زیاد بدون طراحی جداگانه‌ی هر کدوم پوشش داده بشن. مراحل بعدی یکی جابه‌جا شدن (گرافیک الان مرحله ۴، اتصال به مدل مرحله ۵، پرداخت نهایی مرحله ۶). مرحله بعد: تکمیل مرحله ۳ (جمع‌آوری توصیف ظاهر شخصیت از کاربر).
- ۱۴۰۵/۰۶/۲۳ (۲۰۲۶-۰۹-۱۳): دامنه‌ی پروژه گسترش پیدا کرد: (۱) دو نسخه‌ی دختر/پسر قابل‌سوییچ به‌جای یه جنسیت ثابت، (۲) رنگ مو باید داخل اپ قابل‌انتخاب باشه (نه ثابت)، (۳) سبک ظاهر: فانتزی/جادویی، (۴) قابلیت جدید «صدا/گفتار» با Web Speech API مرورگر اضافه شد (بدون سرور جانبی، ولی کیفیت صدای فارسی وابسته به مرورگر/گوشیه و باید رو دستگاه واقعی تست بشه). مراحل بعدی دوباره جابه‌جا شدن: گرافیک = مرحله ۴، صدا = مرحله ۵ (جدید)، اتصال به مدل = مرحله ۶، پرداخت نهایی = مرحله ۷. مرحله بعد: تکمیل جزئیات باقی‌مونده‌ی توصیف شخصیت (رنگ چشم، اکسسوری فانتزی، رنگ لباس، اسم).
- ۱۴۰۵/۰۶/۲۳ (۲۰۲۶-۰۹-۱۳): مرحله ۳ کامل شد. سند CHARACTER.md ساخته شد: رنگ مو/چشم/لباس همه از پنل تنظیمات داخل اپ قابل‌انتخاب‌ان (نه ثابت تو کد)، اکسسوری فانتزی (گوش/بال/هاله/چوب‌دستی) اختیاریه و دیفالتش «هیچ‌کدوم»، دو نسخه‌ی کامل دختر/پسر قابل‌سوییچ، و لیست حالت‌های خلق‌وخو (فعلی + پیشنهادی برای توسعه‌ی بعدی) مستند شد. هنوز کد گرافیکی واقعی نوشته نشده — فقط سند طراحی. مرحله بعد: مرحله ۴ (ساخت واقعی SVG قطعه‌ای بر اساس این سند).
- ۱۴۰۵/۰۶/۲۴ (۲۰۲۶-۰۹-۱۳): مرحله ۴ کامل شد. فایل جدید character.js ساخته شد: SVG قطعه‌ای (صورت پایه + لایه‌های جدا برای ابرو/چشم/دهن/مو/لباس)، دو تابع جدا برای مو و لباس دختر/پسر (buildCharacterSVG با سوییچ gender)، ۴ حالت خلق‌وخو (شاد/خنثی/خسته/عصبانی) هرکدوم به‌صورت گروه‌های .mood-part با data-mood که با کلاس CSS «active» و ترنزیشن opacity کراس‌فید میشن، لایه‌ی پلک‌زدن مستقل (#blink-layer) که هر ۳ تا ۶ ثانیه یه‌بار به‌صورت رندوم چشمک می‌زنه (جدا از انیمیشن حالت‌ها)، و ۴ اکسسوری اختیاری (گوش/بال/هاله/چوب‌دستی) که با دیفالت «هیچ‌کدوم» از دراپ‌داون قابل‌انتخابن. رنگ مو/چشم/لباس و جنسیت و اکسسوری همه تو localStorage (کلید zendenama_character_custom) ذخیره و بین رفرش‌ها حفظ میشن. index.html یه پنل سفارشی‌سازی (سوییچ دختر/پسر + ۳ رنگ‌پیکر + دراپ‌داون اکسسوری) و یه #character-stage گرفت؛ script.js با state.js وصل شد (setMood صدا زده میشه هر بار mood آپدیت میشه). هنوز صدا/گفتار اضافه نشده. مرحله بعد: مرحله ۵ (Web Speech API).
- ۱۴۰۵/۰۶/۲۴ (۲۰۲۶-۰۹-۱۳): کاربر تست کرد (با HTTP Server رو گوشی) و ظاهر گرافیک مرحله ۴ رو ساده/خشک دید — یه پاس بهبود ظاهری بعداً لازمه (خطوط نرم‌تر، تناسب اجزای صورت، فرم مو). هنوز انجام نشده. دامنه‌ی مرحله ۵ هم گسترش پیدا کرد: کاربر گفت صدا باید هم **ورودی** (تشخیص گفتار کاربر، نه فقط خروجی) هم **خروجی** (خوندن جواب شخصیت) داشته باشه، و این اجباریه نه اختیاری. سند CHARACTER.md آپدیت شد تا این رو منعکس کنه. کد مرحله ۵ هنوز نوشته نشده. مرحله بعد: پیاده‌سازی SpeechRecognition (ورودی) + speechSynthesis (خروجی) تو script.js/index.html، و در یه فرصت جدا، بهبود ظاهری گرافیک مرحله ۴.
- ۱۴۰۵/۰۶/۲۴ (۲۰۲۶-۰۹-۱۳): تغییر مهم در ارائه‌دهنده‌ی هوش مصنوعی: کاربر نتونست تو Alibaba Cloud/DashScope ثبت‌نام کنه (ایران تو لیست کشورهای پشتیبانی‌شده برای تأیید امنیتی نبود). بعد از بررسی گزینه‌های جایگزین (DeepSeek مستقیم نیاز به شارژ حساب داره و توکن رایگان نمی‌ده)، تصمیم نهایی: **OpenRouter** جایگزین Qwen/DashScope شد. index.html و script.js آپدیت شدن: پنل تنظیمات حالا فقط «کلید API» و انتخاب مدل داره (Qwen و ریجن حذف شد)، مدل‌ها محدود به دو گزینه‌ی DeepSeek رو OpenRouter (`deepseek/deepseek-chat-v3.1:free` رایگان و کندتر، `deepseek/deepseek-chat` پولی و سریع‌تر) تا کاربر بتونه انتخاب کنه. endpoint ثابت شد رو `https://openrouter.ai/api/v1/chat/completions`. **کلید API و مدل انتخابی حالا تو localStorage (کلید zendenama_settings) ذخیره میشن** و با رفرش/بستن مرورگر از بین نمی‌رن (قبلاً فقط تو حافظه‌ی جلسه بود). این یه تغییر خارج از پلن اصلی مراحل بود، مستقیماً به‌خاطر محدودیت دسترسی از ایران. مرحله بعد: هنوز مرحله ۵ (صدا) باقی مونده؛ کاربر گفته با کلید OpenRouter اول یه تست رایگان می‌کنه.
- ۱۴۰۵/۰۶/۲۴ (۲۰۲۶-۰۹-۱۳): مدل رایگان اولیه‌ی OpenRouter (`deepseek/deepseek-chat-v3.1:free`) توسط OpenRouter حذف/deprecated شد (خطای ۴۰۴). جایگزین شد با `qwen/qwen3.6-plus:free`. برای مقاومت در برابر حذف/کندی مدل‌های رایگان، `callQwen` تو script.js به یه سیستم fallback مجهز شد: به‌جای فرستادن یه `model` ثابت، یه آرایه‌ی `models` (حداکثر ۳ تا — محدودیت خود OpenRouter) فرستاده میشه؛ وقتی کاربر گزینه‌ی «رایگان» رو انتخاب کرده باشه: مدل انتخابی + یه مدل رایگان دیگه (`meta-llama/llama-3.2-3b-instruct:free`) + در آخر یه مدل پولی ارزون (`deepseek/deepseek-chat`) به‌عنوان safety net؛ اگه کاربر مستقیم مدل پولی رو انتخاب کرده باشه، فقط همون یکی فرستاده میشه. تست شد و جواب گرفت (هرچند رایگانش کند بود — پذیرفته شد). **نکته برای ادامه‌دهنده:** لیست مدل‌های رایگان OpenRouter مدام تغییر می‌کنه؛ اگه دوباره خطای ۴۰۴/deprecated دیده شد، لیست `FALLBACK_FREE_MODELS` تو script.js باید آپدیت بشه.
- ۱۴۰۵/۰۶/۲۴ (۲۰۲۶-۰۹-۱۳): مرحله ۵ (صدا) کامل شد. **خروجی صدا:** تابع `speakText()` با `speechSynthesis`، زبان `fa-IR`، سعی می‌کنه بر اساس جنسیت انتخابی (`customization.gender`) یه صدای زنانه/مردانه از بین صداهای فارسی مرورگر انتخاب کنه؛ بعد از هر جواب شخصیت خودکار صدا زده میشه؛ یه چک‌باکس («شخصیت جواب‌ها رو با صدا بخونه»، پیش‌فرض روشن) بالای چت اضافه شد که خاموش/روشنش می‌کنه. **ورودی صدا:** با `SpeechRecognition`/`webkitSpeechRecognition`، یه دکمه‌ی میکروفون (🎤) کنار فیلد پیام اضافه شد که با کلیک شروع/توقف ضبط می‌کنه (حالت «در حال شنیدن» با انیمیشن پالس رو دکمه نشون داده میشه)، متن تشخیص داده‌شده میره تو فیلد پیام (کاربر باید خودش دکمه‌ی ارسال رو بزنه — ارسال خودکار نیست). اگه مرورگر SpeechRecognition رو پشتیبانی نکنه، دکمه‌ی میکروفون غیرفعال میشه و پیام روشنی (نه شکست بی‌صدا) نشون داده میشه. هنوز رو گوشی واقعی با کیفیت صدای فارسی تست نشده — این کار مونده. مرحله بعد: مرحله ۶ (پرداخت/رفع باگ/تست نهایی)، و یه پاس جدا برای بهبود ظاهری گرافیک مرحله ۴ که هنوز انجام نشده.
